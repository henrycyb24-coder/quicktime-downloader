services:
  - type: web
    name: quicktime-downloader
    env: node
    buildCommand: |
      npm install
      apt-get update
      apt-get install -y ffmpeg python3 python3-pip
      pip3 install yt-dlp
    startCommand: node server.js
