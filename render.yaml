services:
  - type: web
    name: quicktime-downloader
    env: node
    buildCommand: npm install
    startCommand: |
      curl -L https://github.com/yt-dlp/yt-dlp/releases/latest/download/yt-dlp -o yt-dlp
      chmod +x yt-dlp
      node server.js
